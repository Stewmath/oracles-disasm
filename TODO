- Text parser:
	- Warn when lines are too long
	- It's not pure ASCII, find exceptions (like tilde)
- Optimize compression algorithms (such as for tilesets/ directory)
- Consider using callab everywhere
