Before Release:

- Dump scripts
- Make tileset compression work
- Makefile:
  - Don't fail when directories don't exist
- Text parser:
	- Warn when lines are too long
	- It's not pure ASCII, find exceptions (like tilde)

Other:

- Documentation:
  - Consider using callab everywhere (replace all calls to interBankCall)
